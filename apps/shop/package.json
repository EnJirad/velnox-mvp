{
  "name": "velnox-shop",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "VelShop — customer-facing Velnox commerce application (deployable from apps/shop).",
  "scripts": {
    "dev": "vite --config ./vite.config.ts",
    "build": "vite build --config ./vite.config.ts",
    "preview": "vite preview --config ./vite.config.ts",
    "typecheck": "tsc -p ./tsconfig.json --noEmit"
  },
  "devDependencies": {
    "vite": "^7.2.6"
  }
}
